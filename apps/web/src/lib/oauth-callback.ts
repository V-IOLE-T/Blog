export const AUTH_CALLBACK_CHANNEL = 'auth-oauth-callback'

/** Set before same-window OAuth redirect (e.g. mobile); consumed by `/auth/social-callback`. */
export const OAUTH_RETURN_PATH_STORAGE_KEY = 'shiro:oauth-return-path'
